<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Asteroid" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/black</item>

        <item name="colorPrimary">@color/pink</item>
        <item name="colorAccent">@color/pink100</item>
        <item name="colorSecondaryContainer">@null</item>
        <item name="android:textColor">@color/pink</item>
        <item name="android:textSize">20sp</item>

        <!--keyboard hides the bottom nav view-->
        <item name="android:windowSoftInputMode">stateAlwaysVisible</item>

    </style>

    <style name="bigText" parent="@android:style/TextAppearance">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="button_stroke" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="strokeColor">@color/pink900</item>
        <item name="strokeWidth">3dp</item>
    </style>

</resources>